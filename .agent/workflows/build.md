---
description: Build the FTC project
---

// turbo-all

## Build Commands

1. Compile debug sources:
```powershell
.\gradlew compileDebugSources
```

2. Full debug build (if needed):
```powershell
.\gradlew assembleDebug
```

3. Clean build:
```powershell
.\gradlew clean compileDebugSources
```
